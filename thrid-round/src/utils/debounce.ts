/* export interface Options {
  maxWait?: number;
  imediate?: boolean;
}

export type Func<A, R> = (...args: A[]) => R;

export const debounce = <A, R>(func: Func<A, R>, wait: number) => {
  let timer: NodeJS.Timeout

  return (...args: A[]): R => {
    if (imediate) {

    }

  }; 
};
 */